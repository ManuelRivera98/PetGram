import styled from 'styled-components';

const Button = styled.button`
    display: block;
    margin: 0 auto;
    background: #8d00ff;
    border-radius: 3px;
    color: #fff;
    height: 32px;
    width: 150px;
`;

export default Button;
